name: Macaron Test CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  analysis:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Macaron Analysis
        uses: ./
        with:
          policy_file: has-hosted-build.dl
          upload_attestation: true
